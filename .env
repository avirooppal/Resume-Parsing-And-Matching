# Frontend and backend URLs for environment configuration

# URL where the frontend is served (update if hosted elsewhere)
FRONTEND_URL=http://localhost:3000

# URL where the backend API is served (update if hosted elsewhere)
BACKEND_URL=http://localhost:8000

# Optionally, you can add more environment variables as needed
